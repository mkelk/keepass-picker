#!/usr/bin/python3
"""Executable shim. The logic lives in keepass_agent.py so tests can import it."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from keepass_agent import main

if __name__ == "__main__":
    main()
